services:
  app:
    image: ${DICTPROP_IMAGE:-dictprop-vps:local}
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - PORT=3000
      - DATA_DIR=/app/data
    stop_grace_period: 3s
    restart: unless-stopped
